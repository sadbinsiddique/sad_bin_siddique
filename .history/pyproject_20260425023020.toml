[project]
name = "sad-bin-siddique-portfolio"
version = "0.1.0"
description = "CSE Student at AIUB (2021–Present). 🚀 Building major projects with a focus on modern architectures. Committed to open-source collaboration and empowering others through code sharing. Turning logic into legacy."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.46.0",
]
